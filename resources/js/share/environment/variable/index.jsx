// Environment shared variables

// Determine whether to update when fetching news
export const newsList = {
    // Whether to update the list. True for update, False for no update
    update: true,
    getNew: true,
    otherNewsList: true,
}
